package net.morimekta.providence.gradle
/**
 * The extension object that define where the providence / thrift files are,
 * and how to handle them.
 */
class ProvidenceTestBuildInfo extends ProvidenceBuildInfo {
    def final String defaultSourcePath = "src/test/providence"
}
